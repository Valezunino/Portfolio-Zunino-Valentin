<section id="inicio" class="hero" aria-labelledby="hero-title">
  <div class="hero-background" aria-hidden="true">
    <span class="hero-glow hero-glow-primary"></span>
    <span class="hero-glow hero-glow-secondary"></span>
  </div>

  <div class="hero-layout">
    <div class="hero-copy">
      <p class="eyebrow">
        <span aria-hidden="true"></span>
        Desarrollador de software
      </p>

      <h1 id="hero-title">
        Convierto ideas en <span>soluciones digitales.</span>
      </h1>

      <p class="description">
        Soy Valentín Zunino, estudiante de Desarrollo de Software. Creo aplicaciones
        web modernas, funcionales y pensadas para brindar una experiencia clara.
      </p>

      <div class="buttons">
        <a href="#proyectos" class="primary">
          Ver mis proyectos
          <svg viewBox="0 0 24 24" aria-hidden="true">
            <path d="M5 12h14M13 6l6 6-6 6"></path>
          </svg>
        </a>

        <a href="#about" class="secondary">Conocer mi perfil</a>
      </div>

      <ul class="hero-highlights" aria-label="Características de mi perfil">
        <li>
          <strong>Full stack</strong>
          <span>Frontend y backend</span>
        </li>
        <li>
          <strong>Responsive</strong>
          <span>Web y móvil</span>
        </li>
        <li>
          <strong>En evolución</strong>
          <span>Aprendizaje continuo</span>
        </li>
      </ul>
    </div>

    <aside class="profile-card" aria-label="Resumen profesional de Valentín Zunino">
      <div class="profile-card-top">
        <span class="profile-monogram" aria-hidden="true">VZ</span>
        <span class="profile-status">
          <span aria-hidden="true"></span>
          Disponible
        </span>
      </div>

      <h2>Valentín Zunino</h2>
      <p>Estudiante de Desarrollo de Software</p>

      <ul>
        <li>
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="m5 12 4 4L19 6"></path></svg>
          Desarrollo web full stack
        </li>
        <li>
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="m5 12 4 4L19 6"></path></svg>
          APIs y sistemas de gestión
        </li>
        <li>
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="m5 12 4 4L19 6"></path></svg>
          Interfaces claras y responsive
        </li>
      </ul>

      <a class="profile-link" href="#contacto">
        Contactar
        <svg viewBox="0 0 24 24" aria-hidden="true">
          <path d="M7 17 17 7M8 7h9v9"></path>
        </svg>
      </a>
    </aside>
  </div>
</section>

<style>
  .hero {
    position: relative;
    display: grid;
    min-height: 94svh;
    place-items: center;
    padding: 9rem 1.5rem 5rem;
    overflow: hidden;
    isolation: isolate;
  }

  .hero-background {
    position: absolute;
    inset: 0;
    z-index: -1;
    overflow: hidden;
    pointer-events: none;
  }

  .hero-background::before {
    position: absolute;
    inset: 0;
    content: "";
    opacity: 0.22;
    background-image:
      linear-gradient(rgba(167, 139, 250, 0.08) 1px, transparent 1px),
      linear-gradient(90deg, rgba(167, 139, 250, 0.08) 1px, transparent 1px);
    background-size: 72px 72px;
    mask-image: linear-gradient(to bottom, black, transparent 82%);
  }

  .hero-glow {
    position: absolute;
    display: block;
    border-radius: 50%;
    filter: blur(110px);
    animation: glow-pulse 8s ease-in-out infinite alternate;
  }

  .hero-glow-primary {
    top: 8%;
    left: 34%;
    width: min(44vw, 680px);
    aspect-ratio: 1;
    background: rgba(124, 58, 237, 0.2);
  }

  .hero-glow-secondary {
    right: 4%;
    bottom: 8%;
    width: min(30vw, 440px);
    aspect-ratio: 1;
    background: rgba(76, 29, 149, 0.16);
    animation-delay: -3s;
  }

  .hero-layout {
    display: grid;
    width: min(100%, 1100px);
    grid-template-columns: minmax(0, 1.22fr) minmax(320px, 0.78fr);
    align-items: center;
    gap: clamp(3rem, 7vw, 6.5rem);
    margin-inline: auto;
  }

  .hero-copy > * {
    opacity: 0;
    animation: reveal-up 700ms cubic-bezier(0.22, 1, 0.36, 1) forwards;
  }

  .hero-copy > :nth-child(2) { animation-delay: 90ms; }
  .hero-copy > :nth-child(3) { animation-delay: 180ms; }
  .hero-copy > :nth-child(4) { animation-delay: 270ms; }
  .hero-copy > :nth-child(5) { animation-delay: 360ms; }

  .eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.65rem;
    margin: 0 0 1.5rem;
    color: #c4b5fd;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .eyebrow > span {
    width: 8px;
    height: 8px;
    background: #8b5cf6;
    border-radius: 50%;
    box-shadow: 0 0 14px #8b5cf6;
  }

  h1 {
    max-width: 760px;
    margin: 0;
    font-size: clamp(3.35rem, 6.3vw, 5.75rem);
    font-weight: 850;
    letter-spacing: -0.065em;
    line-height: 0.98;
  }

  h1 span {
    color: transparent;
    background: linear-gradient(100deg, #c4b5fd 0%, #8b5cf6 45%, #7c3aed 100%);
    background-clip: text;
    -webkit-background-clip: text;
  }

  .description {
    max-width: 640px;
    margin: 1.65rem 0 0;
    color: #a7a7ae;
    font-size: clamp(1rem, 1.7vw, 1.12rem);
    line-height: 1.75;
  }

  .buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
    margin-top: 2rem;
  }

  .buttons a,
  .profile-link {
    display: inline-flex;
    min-height: 50px;
    align-items: center;
    justify-content: center;
    gap: 0.65rem;
    padding: 0.85rem 1.25rem;
    font-size: 0.88rem;
    font-weight: 700;
    text-decoration: none;
    border: 1px solid transparent;
    border-radius: 12px;
    transition: transform 220ms ease, background 220ms ease, border-color 220ms ease, box-shadow 220ms ease;
  }

  .buttons svg,
  .profile-link svg,
  .profile-card li svg {
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.8;
  }

  .buttons svg,
  .profile-link svg {
    width: 18px;
    height: 18px;
  }

  .primary {
    color: white;
    background: #7c3aed;
    border-color: #8b5cf6 !important;
    box-shadow: 0 14px 30px rgba(124, 58, 237, 0.25);
  }

  .secondary {
    color: #e5e7eb;
    background: rgba(255, 255, 255, 0.035);
    border-color: rgba(255, 255, 255, 0.1) !important;
  }

  .buttons a:hover,
  .profile-link:hover {
    transform: translateY(-2px);
  }

  .primary:hover {
    background: #8b5cf6;
    box-shadow: 0 17px 38px rgba(124, 58, 237, 0.34);
  }

  .secondary:hover {
    background: rgba(139, 92, 246, 0.11);
    border-color: rgba(139, 92, 246, 0.38) !important;
  }

  .buttons a:focus-visible,
  .profile-link:focus-visible {
    outline: 3px solid #c4b5fd;
    outline-offset: 4px;
  }

  .hero-highlights {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0;
    max-width: 650px;
    margin: 2.5rem 0 0;
    padding: 0;
    list-style: none;
  }

  .hero-highlights li {
    display: grid;
    gap: 0.3rem;
    padding-inline: 1.2rem;
    border-left: 1px solid rgba(255, 255, 255, 0.09);
  }

  .hero-highlights li:first-child { padding-left: 0; border-left: 0; }

  .hero-highlights strong {
    color: #f4f4f5;
    font-size: 0.88rem;
  }

  .hero-highlights span {
    color: #777780;
    font-size: 0.73rem;
  }

  .profile-card {
    position: relative;
    padding: clamp(1.55rem, 3vw, 2.15rem);
    overflow: hidden;
    background:
      radial-gradient(circle at 100% 0%, rgba(139, 92, 246, 0.21), transparent 38%),
      linear-gradient(145deg, rgba(255, 255, 255, 0.07), rgba(255, 255, 255, 0.025));
    border: 1px solid rgba(167, 139, 250, 0.2);
    border-radius: 28px;
    box-shadow: 0 30px 90px rgba(0, 0, 0, 0.36), inset 0 1px 0 rgba(255, 255, 255, 0.06);
    opacity: 0;
    animation:
      reveal-card 850ms 260ms cubic-bezier(0.22, 1, 0.36, 1) forwards,
      card-float 6s 1.2s ease-in-out infinite alternate;
  }

  .profile-card::after {
    position: absolute;
    top: 0;
    left: 2rem;
    width: 110px;
    height: 1px;
    content: "";
    background: linear-gradient(90deg, transparent, #a78bfa, transparent);
    box-shadow: 0 0 17px rgba(139, 92, 246, 0.9);
  }

  .profile-card-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 2.3rem;
  }

  .profile-monogram {
    display: grid;
    width: 52px;
    height: 52px;
    place-items: center;
    color: white;
    font-weight: 800;
    letter-spacing: -0.05em;
    background: linear-gradient(135deg, #8b5cf6, #5b21b6);
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 15px;
    box-shadow: 0 12px 28px rgba(124, 58, 237, 0.27);
  }

  .profile-status {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.45rem 0.7rem;
    color: #c4b5fd;
    font-size: 0.7rem;
    font-weight: 650;
    background: rgba(124, 58, 237, 0.09);
    border: 1px solid rgba(139, 92, 246, 0.22);
    border-radius: 999px;
  }

  .profile-status > span {
    width: 7px;
    height: 7px;
    background: #8b5cf6;
    border-radius: 50%;
    box-shadow: 0 0 10px #8b5cf6;
  }

  .profile-card h2 {
    margin: 0 0 0.45rem;
    font-size: clamp(1.85rem, 3vw, 2.35rem);
    letter-spacing: -0.045em;
  }

  .profile-card > p {
    margin: 0;
    color: #9b9ba3;
    font-size: 0.9rem;
    line-height: 1.55;
  }

  .profile-card ul {
    display: grid;
    gap: 0.8rem;
    margin: 1.8rem 0;
    padding: 1.4rem 0;
    list-style: none;
    border-top: 1px solid rgba(255, 255, 255, 0.075);
    border-bottom: 1px solid rgba(255, 255, 255, 0.075);
  }

  .profile-card li {
    display: flex;
    align-items: center;
    gap: 0.7rem;
    color: #d5d5da;
    font-size: 0.84rem;
  }

  .profile-card li svg {
    width: 17px;
    height: 17px;
    flex: 0 0 auto;
    color: #a78bfa;
    stroke-width: 2;
  }

  .profile-link {
    width: 100%;
    color: #ddd6fe;
    background: rgba(139, 92, 246, 0.1);
    border-color: rgba(139, 92, 246, 0.26);
  }

  .profile-link:hover {
    background: rgba(139, 92, 246, 0.18);
    border-color: rgba(167, 139, 250, 0.46);
  }

  @keyframes reveal-up {
    from { opacity: 0; transform: translateY(18px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @keyframes reveal-card {
    from { opacity: 0; transform: translateY(24px) scale(0.98); }
    to { opacity: 1; transform: translateY(0) scale(1); }
  }

  @keyframes card-float {
    from { translate: 0 0; }
    to { translate: 0 -6px; }
  }

  @keyframes glow-pulse {
    from { opacity: 0.72; transform: scale(0.96); }
    to { opacity: 1; transform: scale(1.05); }
  }

  @media (max-width: 900px) {
    .hero {
      min-height: auto;
      padding-top: 8.5rem;
    }

    .hero-layout {
      grid-template-columns: 1fr;
      gap: 3.5rem;
    }

    .profile-card {
      width: min(100%, 620px);
    }
  }

  @media (max-width: 580px) {
    .hero { padding: 7.5rem 1rem 3.8rem; }

    .hero-background::before { background-size: 48px 48px; }

    h1 {
      font-size: clamp(3rem, 14vw, 4rem);
      line-height: 1;
    }

    .description { font-size: 0.98rem; }

    .buttons { flex-direction: column; }
    .buttons a { width: 100%; }

    .hero-highlights {
      grid-template-columns: 1fr;
      gap: 0.9rem;
      margin-top: 2rem;
    }

    .hero-highlights li,
    .hero-highlights li:first-child {
      grid-template-columns: 112px 1fr;
      gap: 0.7rem;
      padding: 0 0 0 1rem;
      border-left: 1px solid rgba(167, 139, 250, 0.35);
    }

    .profile-card { border-radius: 23px; }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-copy > *,
    .profile-card,
    .hero-glow {
      opacity: 1;
      animation: none;
      transform: none;
      translate: none;
    }

    .buttons a,
    .profile-link {
      transition: none;
    }
  }
</style>
