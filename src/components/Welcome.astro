<section class="hero">

  <div class="background"></div>

  <p class="badge">
    🚀 Disponible para proyectos freelance
  </p>

  <h1>
    Valentín <span>Zunino</span>
  </h1>

  <p class="description">
    Desarrollo aplicaciones web modernas,
    escalables y enfocadas en la experiencia del usuario.
  </p>

  <div class="buttons">

    <a href="#proyectos" class="primary">
      Ver proyectos
    </a>

    <a href="#contacto" class="secondary">
      Contacto
    </a>

  </div>

</section>

<style>

.hero{
  min-height:100vh;
  display:flex;
  justify-content:center;
  align-items:center;
  flex-direction:column;
  text-align:center;
  position:relative;
  padding:20px;
}

.background{
  position:absolute;
  width:900px;
  height:900px;

  background:
  radial-gradient(
    circle,
    rgba(124,58,237,.25),
    transparent 70%
  );

  filter:blur(120px);

  z-index:-1;
}

.badge{
  padding:12px 20px;

  border-radius:999px;

  background:
  rgba(255,255,255,.04);

  border:
  1px solid rgba(255,255,255,.08);

  color:#9ca3af;

  margin-bottom:2rem;
}

h1{
  font-size:clamp(5rem,10vw,8rem);

  line-height:1;

  font-weight:900;

  letter-spacing:-5px;
}

h1 span{
  background:linear-gradient(
    90deg,
    #8b5cf6,
    #7c3aed
  );

  -webkit-background-clip:text;
  color:transparent;
}

.description{
  max-width:700px;

  margin-top:2rem;

  color:#9ca3af;

  font-size:1.2rem;

  line-height:1.8;
}

.buttons{
  display:flex;
  gap:1rem;
  margin-top:3rem;
}

.buttons a{
  text-decoration:none;
  padding:1rem 2rem;
  border-radius:16px;
  transition:.3s;
  font-weight:600;
}

.primary{
  background:white;
  color:black;
}

.primary:hover{
  transform:translateY(-5px);
  box-shadow:
  0 20px 40px rgba(255,255,255,.1);
}

.secondary{
  border:
  1px solid rgba(255,255,255,.1);

  background:
  rgba(255,255,255,.03);
}

.secondary:hover{
  transform:translateY(-5px);

  border-color:#8b5cf6;

  background:
  rgba(124,58,237,.15);
}

</style>